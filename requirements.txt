# 大学生五育并举访谈智能体 - 依赖列表
# Python版本要求：3.8 - 3.11（请手动检查Python版本）

# 核心依赖
openai>=1.20.0,<2.0.0          # OpenAI SDK（支持百度千帆等兼容接口）

# Web界面依赖
gradio>=4.40.0,<5.0.0          # Web界面框架（避免5.x破坏性更新）
qrcode[pil]>=7.0,<9.0          # 二维码生成

# 数据可视化依赖（第一阶段优化新增）
plotly>=5.14.0                 # 交互式图表库

# 可选依赖（已内置于Python标准库）
# json, datetime, os, random, logging, threading, uuid, socket, sqlite3

# 注意事项：
# 1. 推荐使用虚拟环境安装：python -m venv venv && source venv/bin/activate
# 2. 如果遇到依赖冲突，可使用requirements-lock.txt安装精确版本
# 3. 不支持Python 3.12+（Gradio兼容性问题）
# 4. plotly用于管理后台的数据可视化功能

